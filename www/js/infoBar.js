/**
 * Renders the ammount of resources and deals with countdowns
 * @author Petr Bělohlávek
 *
 */
$(document).ready(function(){

/*
	var cdDialog = $('<div id="countdownBar"/>')
		.html('This dialog will show every time!')
		.dialog({
			autoOpen: false,
			title: 'Basic Dialog'
		});


	$('#countdownBar').click(function(){
		resDialog.dialog('open');

	});


*/


});
