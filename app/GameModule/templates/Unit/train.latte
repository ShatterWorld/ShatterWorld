{block #header}
	Trénink
{/block}

{block #content}


	{form trainUnitForm}

		<!-- Jednoduché vykreslení chyb -->
		<ul class="errors" n:if="$form->hasErrors()">
				<li n:foreach="$form->errors as $error">{$error}</li>
		</ul>

		<table>
			<tr>
				<th>Jméno</th>
				<td>Síla</td>
				<td>Cena</td>
				<td>Množství</td>
				<td>Max</td>
			</tr>
			{foreach $form->getControls() as $name => $field}
				<tr>
					<th class="name">{label $name /}</th>
					<td class="force">
						{dump $units}
						{ifset $units[$name]}$units[$name]->getAttack(){/ifset}

					</td>
					<td class="costs">x</td>
					<td class="amount">{input $name}</td>
					<td class="max">z</td>
				</tr>
			{/foreach}
		</table>

	{/form trainUnitForm}



{/block}

